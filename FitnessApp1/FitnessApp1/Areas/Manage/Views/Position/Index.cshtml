@model List<Position>

@{
    int counter = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-sm-12 col-xl-12">
            <div class="bg-secondary rounded h-100 p-4">
                <h6 class="mb-4">Basic Table</h6>
                <a asp-area="Manage" asp-controller="Position" asp-action="Create" class="btn btn-success">Create</a>
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">psoition Name</th>
                            <th scope="col">Status</th>
                            <th scope="col">Update</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            counter++;
                            <tr>
                                <th scope="row">@counter</th>
                                <td>@item.Name</td>
                                <td><a asp-area="Manage" asp-controller="Position" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger  ">Delete</a></td>
                                <td><a asp-area="Manage" asp-controller="Position" asp-action="edit" asp-route-id="@item.Id" class="btn btn-success  ">Edit</a></td>
                                <td><a asp-area="Manage" asp-controller="Position" asp-action="Detail" asp-route-id="@item.Id" class="btn btn-warning  ">Detail</a></td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>